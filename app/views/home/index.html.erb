<div class="word-list">
<% @words.each do |word| %>
  <strong class="word"><%= link_to word.spelling, word %></strong>
  <% word.definitions.each do |definition| %>
    <p>
      <%= definition.explanation %>
    </p>
  <% end %>
  <hr>
<% end %>
</div>
